# This file reexports PlaylistURLManager from url_history.py for backward compatibility

from core.url_history import PlaylistURLManager

# Reexport the class to maintain backward compatibility
# The imports in main.py and elsewhere will work with either module
